server {
    listen 80;

    # Send all normal web traffic to the React frontend
    location / {
        proxy_pass http://frontend:80;
    }

    # Send any traffic asking for data to the Node backend
    location /api/ {
        proxy_pass http://backend:5000;
    }
}